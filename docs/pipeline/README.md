# Pipeline Guides

这里是 rewrite 后新编译管线的实现 guide 索引。总路线图见 [`../rewrite-roadmap.md`](../rewrite-roadmap.md)，项目结构和类职责见 [`../project-structure.md`](../project-structure.md)，Java/JVM 特性分层见 [`../java-support-tiers.md`](../java-support-tiers.md)，保护/混淆设计见 [`../protection-obfuscation.md`](../protection-obfuscation.md)，输入/配置/输出契约见 [`../io-config-output-contract.md`](../io-config-output-contract.md)。

新主线只写入：

```text
src/main/java
src/test/java
```

旧目录只作为 legacy reference：

```text
obfuscator/src/main/java
obfuscator/src/test/java
obfuscator/bench
```

## 目标管线

```text
ClassFileSource
  -> AsmClassParser
  -> MethodCfgBuilder
  -> ClassHierarchyBuilder
  -> RuntimeAnalysisPipeline
  -> BytecodeToSsaLowerer
  -> OptimizationPipeline
  -> ProtectionPipeline
  -> LlvmModuleLowerer
  -> LlvmProtectionPipeline
  -> LlvmTextEmitter
  -> ZigNativeBuildAndSymbolAudit
  -> Repackager
```

## 分阶段 guide

1. [`00-overview.md`](00-overview.md)：主线编排、source tree、clean-room bootstrap。
2. [`01-classfile-parse.md`](01-classfile-parse.md)：`.class` / JAR 输入与 ASM parse。
3. [`02-method-cfg.md`](02-method-cfg.md)：method-level bytecode CFG。
4. [`03-class-hierarchy.md`](03-class-hierarchy.md)：class hierarchy、method lookup、world model。
5. [`04-callgraph-runtime-analysis.md`](04-callgraph-runtime-analysis.md)：CHA、RTA、points-to、escape、devirtualization。
6. [`05-bytecode-to-ssa.md`](05-bytecode-to-ssa.md)：栈式 bytecode 到三地址 SSA IR。
7. [`06-optimization-passes.md`](06-optimization-passes.md)：method/program optimization pass。
8. [`07-llvm-backend.md`](07-llvm-backend.md)：LLVM IR emission 和 backend 边界。
9. [`08-diagnostics-validation-testing.md`](08-diagnostics-validation-testing.md)：diagnostics、validator、测试矩阵。
10. [`09-debug-dumps-docs.md`](09-debug-dumps-docs.md)：debug dump、可观测性、文档维护。
11. [`10-packaging-native-registration.md`](10-packaging-native-registration.md)：JAR rewrite、loader、native registration、fallback blob。
12. [`11-tier5-runtime-metadata-reflection-jni-unsafe.md`](11-tier5-runtime-metadata-reflection-jni-unsafe.md)：runtime metadata、static reflection、JNI ABI、MethodHandle/ConstantDynamic、Unsafe/VarHandle 前置层。

## 当前语义快照

- SSA merge 已使用 block parameters：branch/goto/switch target arguments 负责传递 predecessor frame value，LLVM backend lower 成 `phi`。
- Exception base 已支持 typed catch handler exception parameter、IR exceptional edge metadata、`THROW` terminator、implicit null/array/cast/div-zero exception site metadata，以及简单 catch-all rethrow / normal+exceptional cleanup shape。multi-exit finally、exception state merge、monitor/finally interaction 等仍精确 `frontendSkipped`。
- Monitor/JMM base 已支持 `monitorenter` / `monitorexit`、`ACC_SYNCHRONIZED` method、识别出的 synchronized exceptional cleanup helper-backed lowering、volatile read/write fence、final field publication fence、monitor happens-before marker、Thread.start/join happens-before helper。`Thread.<init>(Runnable)` / `start` / `join` 和 `Object.wait/notify` 当前保持 JVM helper fallback boundary，不做 native scheduler 或 monitor queue。
- Class initialization skeleton 已支持 active-use guard：跨 owner `getstatic` / `putstatic` / `invokestatic` 和 `new` 插入 class object + guard；同 owner static field / static invoke 不递归 guard，因为当前 owner 已处于初始化语义内。`<clinit>` body 插入 begin/end/failed helper，同 owner `<clinit>` self access 不递归 guard。
- Runtime ABI 已有 catalog/signature/category model，可生成稳定 `runtime-helpers.h` 和 C skeleton；helper reference ABI token 使用 JNI handle 类型（`jobject` / `jclass` / `jarray` / `jthrowable`），C prototype 显式接收 `JNIEnv* env`，LLVM helper declaration 由同一 catalog 映射到 opaque `ptr`。
- JDK bootstrap 已有 `JdkIntrinsicRegistry` policy：Object/String/StringBuilder/System.arraycopy/Math/boxing/Objects 第一批走 direct/runtime helper；当前 host E2E 覆盖 String `isEmpty` / `charAt` / `startsWith` / `endsWith` / `substring(int,int)`、显式 StringBuilder append chain、System.arraycopy byte/int/long/double/object/overlap/null/oob/ArrayStoreException、Integer/Long/Boolean/Double boxing-unboxing、Objects.requireNonNull/equals 和 Math int/long/float/double abs/min/max。ArrayList `add/get/size/contains`、HashMap `put/get/containsKey/overwrite`、`Arrays.copyOf/equals/fill/asList`、`Collections.emptyList/singletonList`、`Optional.of/ofNullable/isPresent/get/orElse`、`String.format(String,Object...)`、Throwable message/cause common paths 和 Thread start/join common paths 当前使用明确 JVM fallback policy，经 encoded fallback 保持 JVM collection/Optional/formatter/array-library/Throwable/Thread 语义；未知 JDK call 走 `halfLowered` JVM helper fallback warning。
- Runtime metadata 已有稳定 index/dump，覆盖 Signature、runtime annotations、record、nest/inner、bridge/synthetic/record generated flags 和 class init/class object handle。Static reflection 可解析 class literal、常量 Class.forName、常量 getDeclaredMethod/Field/Constructor，并把反射 target 接入 call graph reachability；no-arg、reference、primitive 和 array 常量参数 descriptor 的 method/constructor invoke 已进入 helper path，动态字符串/动态参数数组保持 fallback。scan-style reflection such as `getDeclaredMethods/getMethods/getDeclaredFields/getFields/getDeclaredConstructors/getConstructors` 当前用 `REFLECTION_UNSUPPORTED_SCAN` 记录边界。
- JNI 第一层已有 descriptor -> JNI C type、reference/local-frame/pending-exception policy、RegisterNatives table、JNI_OnLoad/bootstrap wrapper plan 和 symbol allowlist。当前 JVM-hosted vertical slice 已能生成 JNI C skeleton、managed Zig `build.zig` workspace、当前 host 动态库、generated loader、SHA-256 校验、`System.load` 和 `JNI_OnLoad` / `RegisterNatives` runtime binding，覆盖 `LLVM_NATIVE_PATH` 的 static/instance primitive scalar add/long/double/boolean compare/void no-op、if/else、nested if、block-parameter/phi merge、table/lookup switch、JVM numeric helper opcodes、static/instance/volatile field helper path、monitor/synchronized helper path、explicit `athrow` -> JNI `Throw` bridge、static reflection method/constructor/field helper path（含常量参数 method/constructor invoke）、Unsafe `objectFieldOffset` token / `getInt` / `putInt` / monitor-backed `compareAndSwapInt` / `allocateInstance` helper path、typed-int VarHandle helper path、div/rem ArithmeticException helper、String/reference field pass-through、broad primitive/reference array helper subset、System.arraycopy byte/int/long/double/object/overlap/null/oob/ArrayStoreException helper、selected primitive/reference array allocation helpers、ordinary-method object construction helper subset、`checkcast` / `instanceof` helper subset、String `length` / `equals` helpers、StringConcatFactory `makeConcat` / common `makeConcatWithConstants` helper path、LambdaMetafactory common `metafactory` helper path、LDC MethodHandle + `invokeExact` direct path、Math `abs/min/max` int/long helper subset、same-class selected static/private-special direct call，以及 tokenized virtual/interface JVM dispatch helper 的 no-arg int、int-arg int 和 reference-return subset。dispatch helper 使用 JNI `Call<Type>Method`，不实现 native vtable。`TEMPLATE_JNI_PATH` / helper path 仍覆盖 String content JNI path、`int[]` copy/new-array API、`ThrowNew` exception bridge smoke path、generic straight-line/simple-branch constructor/class-initializer body helper first layer 和更广泛 helper/fallback 边界。
- Native build 的正式路径是 managed Zig `0.15.2` 统一接管 `.ll`、Zig-managed `.o`、JNI wrapper C、runtime helper C 和 fallback blob carrier sources。Zig 安装目录固定为可执行 `j2ll.jar` 同级的 `zig/zig(.exe)`；缺失时先使用同目录已有官方 Zig archive，仍缺失再从 Zig 官方 download path 下载；local/downloaded archive 必须按内置官方 SHA-256 metadata 校验，通过后才将官方 archive 根目录内容规范化到 `zig/`，checksum mismatch 是 toolchain failure。Signature verification 当前明确报告 `notVerifiedBoundary`。Java 侧只生成一个 `build.zig` workspace 并执行 managed `zig build`；不要新增 host `cc` / `clang` / `zig cc` ad-hoc path。Packaging report 的 `zigToolchain.bootstrapEvents` 记录 found/reinstall/local-archive/download/checksum/install 等用户可诊断事件，并包含 archiveName/archiveSha256/checksumStatus/signatureStatus/source。
- invokedynamic base 已支持 StringConcatFactory `makeConcat` 到真实 StringBuilder helper E2E；`makeConcatWithConstants` 常见 recipe 已通过 tokenized native constant carrier + StringBuilder helper E2E。LambdaMetafactory `metafactory` common path 已通过 JVM `LambdaMetafactory` helper E2E；MethodHandle `bindTo` / `asType` / `dropArguments` adapter chain、`permuteArguments` / `filterArguments` / `foldArguments` reason-split fallback、collector-style `METHOD_HANDLE_COLLECTOR_UNSUPPORTED` adapter 和 unsupported `altMetafactory` two-capture serializable lambda 已通过 encoded bytecode-preserving fallback E2E。复杂 MethodHandle interpreter、完整 altMetafactory runtime class semantics 或 unsupported ConstantDynamic 仍按 skeleton/fallback 边界处理。
- Unsafe/VarHandle bounded subset 已 helper-backed lowering，并对 volatile/CAS 发 JMM marker；当前真实 E2E 覆盖 statically resolved field-token `Unsafe` int access/CAS、`allocateInstance` 和 typed-int VarHandle get/set/volatile/CAS，offset/token 不表示 native object address；unsupported memory access API 走明确 fallback 诊断。
- catch-all/finally 已支持简单 rethrow 和 normal/exceptional cleanup shape；multi-exit/state-merge/monitor-finally interaction 仍精确 `frontendSkipped`。
- Runtime metadata/static reflection/JNI ABI/Unsafe/helper facts 已进入主线 reports：runtime metadata dump 写入 `intermediates/runtime/runtime-metadata.json`，lowering report 的 `helperBackedSites` 标注 `RUNTIME_METADATA_HELPER`、`REFLECTION_HELPER`、`REFLECTION_FIELD_HELPER`、`REFLECTION_METHOD_HELPER`、`REFLECTION_CONSTRUCTOR_HELPER`、`REFLECTION_ACCESSIBLE_HELPER`、`UNSAFE_HELPER`、`VARHANDLE_HELPER`、`JNI_ABI_REGISTER_NATIVES`、`FIELD_HELPER`、`ARRAY_HELPER`、`ARRAYCOPY_HELPER`、`ALLOCATION_HELPER`、`TYPE_HELPER`、`STRING_HELPER`、`STRING_BUILDER_HELPER`、`STRING_CONCAT_CONSTANTS_HELPER`、`LAMBDA_METAFACTORY_HELPER`、`JDK_INTRINSIC_HELPER`、`JDK_COLLECTION_HELPER`、`THROWABLE_HELPER`、`THREAD_HELPER`、`WAIT_NOTIFY_FALLBACK`、`JMM_FENCE`、`MONITOR_HELPER`、`SYNCHRONIZED_METHOD_HELPER`、`EXCEPTION_HELPER`、`DIV_REM_EXCEPTION_HELPER`、`DIRECT_LLVM_CALL`、`JVM_CALL_HELPER`、`DISPATCH_HELPER`、`DEFAULT_INTERFACE_DISPATCH_HELPER`、`DEFAULT_INTERFACE_DISPATCH_FALLBACK`、`UNSUPPORTED_DEFAULT_INTERFACE_CONFLICT`、`UNSUPPORTED_DEFAULT_INTERFACE_SUPER`、`DEFERRED_DISPATCH_HELPER`、`CONSTRUCTOR_CALL_HELPER`、`CONSTRUCTOR_BODY_HELPER` 和 `CLASS_INITIALIZER_BODY_HELPER`。Packaging report 的 fallback blob 记录 `fallbackInvokeDescriptor`、`fallbackReasonCode`、`FALLBACK_HIDDEN_CLASS` / `FALLBACK_DEFINE_CLASS` / `FALLBACK_HIDDEN_CLASS_UNAVAILABLE` / `FALLBACK_HIDDEN_CLASS_UNSUPPORTED_ACCESS` definition capability reason code、`FALLBACK_CACHE_REUSE` cache reason，以及 `cacheScope` / `cacheKey` / `cacheLifetime` / `globalReferencePolicy` / `unloadAware=false` / `futurePath` lifecycle 字段；target preflight 通过 diagnostics/report 记录 `ZIG_TARGET_PREFLIGHT` 或 required-target failure `ZIG_TARGET_UNBUILDABLE`，并带 required、Zig triple、failure kind、SDK requirement 和 build log tail；packaging report 还记录 JAR preservation summary 和 signature action。
- `nativeEmbeddedClassBlob` fallback 目前有稳定 planner/report 和真实 ordinary-method bytecode body path：helper class bytes 不作为 JAR 明文 `.class` entry 输出，而是以 v1 `j2ll-rle-byte-pairs-v1` + `xor-sha256-key-stream-v1` encoded blob 编入 host native artifact。对于可 JNI 桥接的 ordinary `halfLowered` 方法，packaging 会把原 method body 复制为同 owner package hidden/helper class 的 static synthetic `invoke` 方法；instance fallback 把 `this` 作为第一个参数传入，static fallback 使用 owner `jclass`，primitive/reference 参数和返回通过 JNI `CallStatic<Type>Method` 桥接，fallback body 抛出的 pending exception 直接传播。JNI 在 native-side SHA-256 校验后解码，再通过打包进 output JAR 的 `J2llFallbackSupport` 获取 owner-private `MethodHandles.Lookup` 并优先 `defineHiddenClass` lazy define/reuse。JDK 或 owner-private Lookup 不满足时选择已实现的 JNI `DefineClass` 兼容机制并记录 `FALLBACK_HIDDEN_CLASS_UNAVAILABLE` / `FALLBACK_HIDDEN_CLASS_UNSUPPORTED_ACCESS`。decoder 对 encoded hash、wrong key/fallback id 和 corrupted RLE payload 有 bounded validation，避免 malformed blob 触发 unbounded allocation。runtime cache 是 fallback id + classloader identity 的 process-lifetime linked global-ref cache；report 明确 `unloadAware=false`，不承诺 classloader unload 释放 native global-ref cache；child JVM E2E 已覆盖同一 output JAR 经两个独立 `URLClassLoader` 加载时的隔离注册和 fallback 调用。
- 最小 JVM-hosted E2E 当前已接实当前 host target vertical slice：original input JAR 与 output JAR 都在 child JVM 中运行，output JAR 内嵌 managed Zig build 产出的当前 host 动态库，generated loader 真实 `System.load`，`JNI_OnLoad` 执行 `RegisterNatives`。E2E 覆盖 `LLVM_NATIVE_PATH` 的 static primitive scalar add/long/double/boolean compare/void no-op、if/else、nested if、phi merge、table/lookup switch、JVM numeric helper opcodes、static/instance/volatile field helper path、monitor/synchronized helper path、explicit throw bridge、static reflection method/constructor/field/setAccessible helper path（含常量参数 method/constructor invoke、typed Field int/boolean/long/double/reference accessor、private method/constructor accessible smoke）、Unsafe statically resolved int field token helper path、typed-int VarHandle helper path、direct static/private-special callee call、direct-call hidden table indirection、LDC MethodHandle direct `invokeExact`、div/rem ArithmeticException helper、String/reference field pass-through、broad primitive/reference array helper subset、System.arraycopy byte/int/long/double/object/overlap/null/oob/ArrayStoreException helper、selected primitive/reference array allocation helpers、ordinary-method object construction helper subset、`checkcast` / `instanceof` helper subset、String `length` / `equals` helpers、StringConcatFactory `makeConcat` / `makeConcatWithConstants`、LambdaMetafactory common `metafactory` helper、Math int/long helper subset 和 tokenized virtual/interface/default-interface JVM dispatch helper 的 no-arg int、int-arg int、reference-return subset，和 template/helper path 的 multi-class/multi-method registration、String content JNI path、primitive int array copy/new-array path、exception bridge smoke、generic straight-line/simple-branch constructor/class-initializer body helper first layer，以及 encoded fallback 的 unsupported JDK call、ArrayList/HashMap/Arrays/Collections/Optional/String.format narrow policy、Throwable message/cause common path、Thread start/join common path、wait/notify boundary、dynamic Class.forName / dynamic getDeclaredMethod / dynamic parameter array reflection、reflection scan boundary、MethodHandle adapter chain（含 `bindTo` / `asType` / `dropArguments` / `permuteArguments` / `filterArguments` / `foldArguments` reason split）、collector-style unsupported adapter、unsupported altMetafactory capture shape、instance receiver/reference return、exception propagation 和 two-classloader isolation；default-interface super `I.super.m()` 当前作为 `UNSUPPORTED_DEFAULT_INTERFACE_SUPER` frontendSkipped no-rewrite boundary 覆盖。selected target matrix 的 build plan、layout、preflight 和 report 已稳定；schema v1 selected target 默认 required，当前 preflight 不可构建的 required target 进入 `failedTargets` 并用 `ZIG_TARGET_UNBUILDABLE` 使 pipeline failed，真实交叉编译成功与否由后续 managed Zig capability/SDK 接实决定。
- Release-readiness reporting now includes `reports/artifact-audit.json`, `reports/support-matrix.json`, `reports/opcode-support-matrix.json`, `reports/known-blockers.json`, `reports/release-readiness.json`, `reports/index.json`, `reports/summary.md` and `reports/summary.json` with deterministic artifact checks, feature/opcode/status/reason/test coverage rows, `coverageLevel` / `evidenceCount`, explicit blocker ids, native target buildable/unbuildable summary and a short user-facing aggregate status. `reports/index.json` v2 records path/reportVersion/SHA plus beta/RC/readiness/failure-required flags, and readiness validates required report hashes plus final JAR reports-manifest consistency. Artifact audit v2.2 records checked/observed/skipped hash-only sensitive plaintext facts plus generated C, helper C, per-class LLVM, build.zig, native-resource, output-JAR, symbol-audit, packaging-report, fallback binary audit and final JAR metadata surface coverage; connected `LLVM_NATIVE_PATH` facts, stable TEMPLATE constructor/body helper string facts (`TEMPLATE_JNI_PATH_STABLE_SURFACE`) and StringConcat constant carrier stable generated-C facts are blocking when the literal is specific enough for stable audit, while short/common literals and reflection/lambda/MethodHandle metadata facts remain observed-only evidence. Fallback blob audit checks encoded/original SHA, size/encoding policy and original method key plaintext absence from carrier C, but does not enable a broad plaintext literal gate for complex fallback blobs yet. The readiness gate checks diagnostics, artifact audit, frontend skip, lowering, packaging, protection, symbol audit, support/opcode matrix and known blockers for required fields; strict suite mode also requires `reports/release-suite-summary.json` with stable suite/case metadata, `profile` (`smoke`/`standard`/`beta`/`rc`), `requiredCategories`, `missingCategories`, aggregate, determinism evidence, expected support evidence/report locations and original/output child JVM results. Beta profile requires CLI artifact smoke, docs examples validation, report index, minimal LLVM native and mixed helper/fallback evidence; uncovered `beta-blocker` reasons make `betaProfilePassed=false` and populate `betaMissingEvidence`. RC profile requires no missing categories and covers minimal LLVM native, mixed helper/fallback/protection, signed strip/resign, ServiceLoader/multi-release/module-info, config expected failure, artifact-audit expected failure, required non-host target expected failure, determinism, known blocker evidence and realistic CLI/reflection/packaging sample evidence. Gate v6 verifies release-blocking `beta-blocker` / `rc-blocker` reasons have support/opcode matrix coverage and release-suite expected-status/diagnostic evidence or weird-bytecode seed coverage; `future-blocker` and `non-goal` rows remain visible but do not fail beta/RC strict readiness. It rejects expected failure cases that still have output runs, validates selected target evidence, requires `determinismEvidenceComplete`, and writes `missingEvidence`, `suiteCoverageByBlocker`, `blockerEvidenceComplete`, `targetEvidenceComplete`, `finalArtifactWritten`, `determinismEvidenceComplete`, `metadataConsistencyPassed`, `blockingSensitiveFactsPassed`, `targetPackagePlanComplete` and `strictModePassed`. `reports/packaging-report.json` includes `zigToolchain.bootstrapEvents` archive/checksum/signature/source evidence and `zigToolchain.targetArtifacts` with OS/arch classifier, library extension, expected artifact path/name/resource path, loader extraction policy, symbol visibility policy, actual current-host SHA-256/exported symbols, failed-target reason/capability/SDK/failure-kind/build-log-tail evidence and Windows PDB exclusion policy. CLI now supports `validate` and `dry-run`; `dry-run` writes reports without Zig/native build or final JAR. Gradle `cliJar` builds `build/cli/j2ll.jar`, `distJ2ll` assembles `build/dist/j2ll/` with jar/docs/examples/samples/getting-started, and `betaAcceptance` runs the dist jar through help/version/validate/dry-run/build plus child JVM output verification. Final JARs include `META-INF/j2ll/build-info.json`, `native-libraries.json` and `reports-manifest.json`, with hashes only for config/protection/report/native identity and expected `index.json` / `summary.md` report names.
- Protection v1 已有真实接线：IR protection 默认启用 String constant carrier encryption、primitive int/long constant XOR split、safe single-block fake branch/basic-block splitting 和 block-name obfuscation；monitor/JMM/exception/call/field/helper-sensitive shape 以及 `<init>` / `<clinit>` body helper shape 按 pass 记录 skip reason，不改变 lowering status。LLVM name obfuscation 通过共享 `LlvmNameMangler` 贯通 planner、LLVM lowerer、Zig workspace 和 JNI wrapper，生成 hidden `j2ll_f_<sha256>` linkable symbols；LLVM 混淆仍必须基于 LLVM module model，不能做 `.ll` 文本 regex。

## 维护规则

- 新增 stage、fallback 策略、validator、测试落点或目录边界时，先更新 `AGENTS.md`，再更新对应 stage guide。
- README 保持用户视角；内部 rewrite 计划和 compiler 设计只放在 `docs/`。
- 单个 stage guide 应保持 focused。跨阶段规则放在本索引、`00-overview.md` 或 `08-diagnostics-validation-testing.md`。
